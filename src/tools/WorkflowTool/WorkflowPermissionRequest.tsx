// Stub for WorkflowPermissionRequest
import React from 'react';

export function WorkflowPermissionRequest(): React.ReactElement {
  return React.createElement('div', null, 'Workflow Permission Request not available');
}
export default WorkflowPermissionRequest;
